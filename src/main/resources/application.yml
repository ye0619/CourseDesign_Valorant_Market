server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ? ??????????????????
    url: jdbc:mysql://localhost:3306/valorant_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: "" # ? ???????? (?? 123456)

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true # ???????? (display_name -> displayName)

# 在文件最下方添加
logging:
  level:
    com.example.demo.mapper: debug  # 打印你的 Mapper 接口执行的 SQL